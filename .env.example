# Rackd Configuration Example
# Copy this file to .env and adjust values as needed

# Data directory for device storage/database
RACKD_DATA_DIR=./data

# Server listen address
RACKD_LISTEN_ADDR=:8080

# MCP authentication token (optional, leave empty to disable)
RACKD_BEARER_TOKEN=

# Storage backend: sqlite or file (default: sqlite)
# Note: Relationships feature requires SQLite backend
RACKD_STORAGE_BACKEND=sqlite

# Storage format for file backend: json or toml (default: json)
# Only used when RACKD_STORAGE_BACKEND=file
RACKD_STORAGE_FORMAT=json
